<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Detail</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .author-name {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .quote {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 10px;
            background-color: #f9f9f9;
            border-left: 5px solid #3498db;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #00000065;">
        <a class="navbar-brand" href="#">
            <img src="assets/logo.png" width="40" height="30" class="d-inline-block align-top" alt="">
            Quotes
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li>
                <li>
                    <a href="/authors/<%= authors.id %>/addQuote">Add Quote</a
                </li>
            </ul>
        </div>
    </nav>
    <div class="container">
        <h1 class="author-name"><%= authors.authorName %></h1>
        <div class="quotes">
            <% authors.Quotes.forEach(quot => { %>
             <div class="quote">
                <%= quot.quote %>
                <% console.log(quot) %>
                <div>
                    <a href="/authors/<%= authors.id %>/<%= quot.id %>/edit">Edit</a>
                    <a href="/authors/<%= authors.id %>/<%= quot.id %>/delete">Delete</a>
                </div>
             </div>
            <% }) %>
        </div>
    </div>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
