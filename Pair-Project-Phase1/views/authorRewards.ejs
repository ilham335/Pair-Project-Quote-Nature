<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Author Rewards</title>
</head>
<body>
    <h1>Author Rewards</h1>
    <% if (author) { %>
        <h2><%= author.authorName %></h2>
        <ul>
            <% author.Rewards.forEach(reward => { %>
                <li><%= reward.name %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No data available</p>
    <% } %>
</body>
</html>